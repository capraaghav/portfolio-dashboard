# Personal data — never share or commit (holdings, snapshots, watchlist, prices)
# Ignore data/ contents (not the dir itself) so the bundled sample can be re-included.
data/*
# …except the bundled onboarding sample portfolio (no real user data)
!data/sample/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Streamlit secrets (if ever added)
.streamlit/secrets.toml

# Local agent/skill tooling (not app code)
.agents/
.claude/
.claude-flow/
.codex/
.gemini/
skills-lock.json
.impeccable/
.gstack/
